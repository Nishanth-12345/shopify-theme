<html>
  <head>
    {{ content_for_header }}
    {{ 'style.css' | asset_url | stylesheet_tag}}
  </head>
  <body>
    {% section 'announcement-bar' %}
    {{ content_for_layout }}
    {{ product.category }}
    {{ 'scripts.js' | asset_url | script_tag}}
  </body>
</html>
